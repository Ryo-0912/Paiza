テストが遠らないところあり

```
<?php
    $n = (int)fgets(STDIN);
    
    
    for($i=0;$i<$n;$i++)
    {
        $array = explode('.' , trim(fgets(STDIN)));
        $str_array= implode($array);
        if(ctype_digit($str_array) && count($array) == 4) {
            echo("True\n");
        } else {
            echo("False\n");
        }
        
    }
?>
```
