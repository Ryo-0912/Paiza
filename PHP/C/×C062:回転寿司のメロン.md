例は回答が合うが、テストケースは一つも結果が合わない(未解決)

```
<?php
    $n = trim(fgets(STDIN));
    
    $list = [];
    
    for($i=0;$i<$n;$i++)
    {
        $list[$i+1] = trim(fgets(STDIN));
    }
    
    $melon_list = [];
    $plus_ten = [];
    foreach ($list as $key => $l)
    {
        if($l == 'melon')
        {
            $melon_list[] = $key; 
        }
    }
    
    $count = count($melon_list);
    
    foreach ($melon_list as $ml)
    {
        if($melon_list[$count-1] >= $ml+10)
        {
            $plus_ten[] = $ml+10;
        }
    }
   
   $result = [];
   foreach ($plus_ten as $pt)
   {
       foreach ($melon_list as $ml)
       {
           if($pt-10 == $ml)
           {
               $result[] = $ml;
           } elseif($ml>$pt && $pt-10 != $ml)
           {
              $result[] = $ml;
           }
       }
   }

   print_r(count($result));
?>
```
